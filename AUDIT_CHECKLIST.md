# 🛡️ CGS / Zuna HRMS — Production Audit & Hardening Checklist (Phase 1)

**Date:** September 8, 2026  
**Auditor:** Antigravity Autonomous Diagnostic Engine  
**Evidence Standard:** Strict Evidence-Gated (§0 Protocol) — every issue backed by file/line, live query output, or build logs.

---

## 1. Executive Issue Summary

| Severity | Definition | Count |
| :--- | :--- | :---: |
| 🔴 **Critical** | Data loss, authentication bypass, tenant leakage, privilege escalation, build-breaking syntax | **7** |
| 🟠 **High** | Missing authorization on sensitive mutations, unhandled SQL/schema mismatch, lack of rate limiting | **6** |
| 🟡 **Medium** | Missing DB transactions on multi-step workflows, CORS wildcard, unhandled error leaking | **5** |
| 🟢 **Low** | Dead code, legacy package bloat, missing logging context | **3** |
| **Total** | | **21** |

---

## 2. Issue Checklist

| # | Severity | Module | File : Line | Issue Description | Direct Evidence | Proposed Fix | Risk if Unfixed | Risk of Fixing |
| :-: | :---: | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | 🔴 Critical | **Client / Profile** | [client/src/pages/Profile.jsx:9](file:///c:/HRMS/client/src/pages/Profile.jsx#L9) | Syntax error: `changeUserPassw ord` has a space inside identifier, breaking Vite build completely. | `npm run build` failed with `[builtin:vite-transform] Expected ',' or '}' but found 'Identifier' at Profile.jsx:9:64`. | Correct identifier to `changeUserPassword`. | Frontend cannot be built or deployed to Vercel. | Zero. Trivial 1-word syntax fix. |
| **2** | 🔴 Critical | **Auth Middleware** | [server/src/middlewares/auth.js:9-18](file:///c:/HRMS/server/src/middlewares/auth.js#L9-L18), [23-33](file:///c:/HRMS/server/src/middlewares/auth.js#L23-L33) | Complete authentication bypass & admin privilege escalation: If no token or invalid token is supplied, passing `?companyId=...` or `{ companyId: ... }` automatically assigns `req.user = { id: ..., companyId: ..., role: 'admin' }` and passes `next()`. | Code inspection of lines 9-18 and 23-33 in `auth.js`. Any unauthenticated request with `?companyId=x` bypasses authentication and gains admin privileges. | Remove unauthenticated `companyId` fallback entirely. Require genuine JWT verification. If test scripts need API access, provide valid bearer token or dedicated internal service key. | Total security bypass: any anonymous internet visitor can perform admin actions by adding `?companyId=...`. | Very low; client already passes `Authorization: Bearer <token>` via `apiFetch()`. |
| **3** | 🔴 Critical | **Multi-Tenancy** | [server/src/controllers/userController.js:6-22](file:///c:/HRMS/server/src/controllers/userController.js#L6-L22) | Cross-tenant user enumeration: `getUsers` takes `targetCompanyId = companyId || req.user?.companyId`. If an employee of Company A passes `?companyId=CompanyB`, the server returns all Company B users. | In `userController.js:7`, `companyId` query parameter overrides `req.user.companyId` without checking if `req.user.role === 'superadmin'`. | Enforce tenant isolation: `const targetCompanyId = (req.user?.role === 'superadmin' && companyId) ? companyId : req.user?.companyId`. | Complete cross-tenant data leak of employee PII, salaries, emails, and phone numbers. | Zero; legitimate tenant users only query their own company. |
| **4** | 🔴 Critical | **Authorization / IDOR** | [server/src/controllers/userController.js:98-142](file:///c:/HRMS/server/src/controllers/userController.js#L98-L142) | Privilege escalation in `updateUser`: Any user can update their own (or another user's) `role` to `admin` or `superadmin`, alter `gross_salary`, or switch `company_id`. | In `allowedFields` array (lines 98-103), `"role"`, `"company_id"`, and `"gross_salary"` are unrestricted. There is no check if caller is an admin or editing self. | Restrict sensitive field updates (`role`, `company_id`, `gross_salary`, `casual_leave_quota`, etc.) to `req.user.role === 'admin' \|\| req.user.role === 'superadmin'`. For normal employees, only allow profile fields (`name`, `phone`, `avatar_url`, `skills`). | Any employee can promote themselves to superadmin and increase their salary arbitrarily. | Low; admins retain full edit rights, employees retain profile editing. |
| **5** | 🔴 Critical | **Authorization / IDOR** | [server/src/controllers/userController.js:168-195](file:///c:/HRMS/server/src/controllers/userController.js#L168-L195) | Unrestricted user deletion: `deleteUser` does not check caller role or company boundary. Any employee can delete any user, including admins. | In `userController.js:168`, `deleteUser` has no check on `req.user.role` or `req.user.companyId`. | Require `req.user.role === 'admin' \|\| req.user.role === 'superadmin'`. Also verify that the target user belongs to `req.user.companyId` (unless superadmin). | Malicious or accidental deletion of users, admins, and cascading records. | Zero; employees should never have user deletion rights. |
| **6** | 🔴 Critical | **Chat / DMs** | [server/src/controllers/chatController.js:382-394](file:///c:/HRMS/server/src/controllers/chatController.js#L382-L394), [290-307](file:///c:/HRMS/server/src/controllers/chatController.js#L290-L307) | Direct message privacy breach (IDOR): `getDirectMessages` does not check if `req.user.id` is a participant in `threadId`. In `getDmThreads`, passing `?userId=...` returns other users' DM threads. | Line 385 queries `direct_messages WHERE thread_id = $1` with zero participant verification. Line 293 takes `targetUserId = userId \|\| req.user?.id`. | Validate that `req.user.id` is listed in `dm_threads.participants` before returning messages. Prevent non-superadmins from querying another user's DM list. | Any employee can eavesdrop on private conversations between any other employees or executives. | Zero; legitimate participants are in `participants` array. |
| **7** | 🔴 Critical | **Attendance Regularization** | [server/src/controllers/regularizationController.js:212-220](file:///c:/HRMS/server/src/controllers/regularizationController.js#L212-L220) | SQL Runtime Crash: `updateRegularizationStatus` attempts to insert/update non-existent column `total_working_minutes` in `attendance` table when approving requests. | Live DB check on `attendance` columns confirmed `duration_minutes` exists, `total_working_minutes` does NOT exist. Approval query will fail with `column total_working_minutes of relation attendance does not exist`. | Replace `total_working_minutes` with `duration_minutes` in the SQL query. | When managers click "Approve" on attendance regularization, the request fails with 500 error and attendance is not updated. | Low; fixes broken database query to match actual schema. |
| **8** | 🟠 High | **Leaves Authorization** | [server/src/controllers/leaveController.js:182-202](file:///c:/HRMS/server/src/controllers/leaveController.js#L182-L202) | Self-approval of leaves: `updateLeaveStatus` does not verify if the caller is an `admin`, `superadmin`, or `manager`. An employee can approve their own leave requests. | In `leaveController.js:182`, `updateLeaveStatus` accepts `status` from `req.body` and updates the record with no role check. | Require `['admin', 'superadmin', 'manager'].includes(req.user?.role)`. Prevent employees from approving/rejecting leave requests. | Employees can approve their own leaves without manager oversight. | Zero; leave approvals are inherently managerial actions. |
| **9** | 🟠 High | **Payroll Authorization** | [server/src/routes/payroll.routes.js:13-17](file:///c:/HRMS/server/src/routes/payroll.routes.js#L13-L17), [server/src/controllers/payrollController.js:286-314](file:///c:/HRMS/server/src/controllers/payrollController.js#L286-L314) | Unrestricted payroll deletion & mutation: `DELETE /api/payroll/all` and `POST /api/payroll` have no `requireAdmin`. Any employee can call `wipeAllPayrolls` and delete all company payroll data. | `payroll.routes.js` mounts `wipeAllPayrolls` directly under `authenticateToken` with no role check. `wipeAllPayrolls` executes `DELETE FROM payroll WHERE 1=1`. | Add `requireAdmin` middleware to `POST /api/payroll`, `DELETE /api/payroll/all`, `DELETE /api/payroll/:id`. Enforce `req.user.companyId` on all payroll deletions. | Malicious employee could wipe all statutory payroll records for the entire organization. | Zero; payroll generation/deletion is an administrative function. |
| **10** | 🟠 High | **Company Management** | [server/src/controllers/companyController.js:82-110](file:///c:/HRMS/server/src/controllers/companyController.js#L82-L110) | Unrestricted company modification: `updateCompany` does not verify if the caller is an admin of that company or a superadmin. | In `companyController.js:82`, any authenticated user can change company name, logo, settings, and modules. | Restrict `updateCompany` to `superadmin` or `admin` where `req.user.companyId === id`. | Any employee can rename companies, change module access, or tamper with company settings. | Zero; company settings belong to admins. |
| **11** | 🟠 High | **External Client Links** | [server/src/routes/externalLinks.routes.js:16-19](file:///c:/HRMS/server/src/routes/externalLinks.routes.js#L16-L19) | External client links management protected only with `optionalAuth`. Anonymous users can list, create, or revoke external client chat links. | `router.get("/", optionalAuth, getExternalLinks)` and `router.delete("/:id", optionalAuth, revokeExternalLink)`. | Change `optionalAuth` to `authenticateToken` for management routes (`GET /`, `POST /`, `PATCH /:id/revoke`, `DELETE /:id`). Only keep `GET /token/:token` public. | Unauthenticated attackers can view client names, project names, and revoke client communication gateways. | Zero; administrative endpoints must be authenticated. |
| **12** | 🟠 High | **Rate Limiting** | [server/src/app.js:1-48](file:///c:/HRMS/server/src/app.js#L1-L48) | Missing rate limiting on authentication and email-triggering routes (`/api/auth/login`, `/api/auth/forgot-password`, `/api/auth/register`). | No rate-limiting middleware found in `server/src/app.js` or `package.json`. | Implement lightweight in-memory rate limiting middleware for auth routes (e.g. max 10 attempts per 15 min for login/forgot-password). | Credential brute-forcing, denial of service on Gmail SMTP quota, and email spamming. | Low; tuned to 10-20 requests/window, will not affect normal user usage. |
| **13** | 🟠 High | **Asset Management** | [server/src/routes/assets.routes.js:13-16](file:///c:/HRMS/server/src/routes/assets.routes.js#L13-L16) | Asset mutations (`POST /`, `PATCH /:id`, `DELETE /:id`) have no admin check. Any employee can reassign, create, or delete company hardware/software assets. | In `assets.routes.js`, all routes use only `authenticateToken`. `assetController.js` does not check role. | Require `requireAdmin` for `createAsset`, `updateAsset`, `deleteAsset`. Allow normal employees read-only access (`GET /`). | Employees can delete asset inventory or reassign laptops to arbitrary users without permission. | Zero; asset tracking is an IT admin role. |
| **14** | 🟡 Medium | **DB Transactions** | [server/src/controllers/userController.js:168-190](file:///c:/HRMS/server/src/controllers/userController.js#L168-L190), [server/src/controllers/projectController.js:194-206](file:///c:/HRMS/server/src/controllers/projectController.js#L194-L206) | Multi-query deletion operations not wrapped in SQL transactions (`BEGIN / COMMIT / ROLLBACK`). | In `deleteUser`: 7 sequential `UPDATE` queries followed by 2 `DELETE` queries. If query #8 fails, database is left in a corrupted, half-deleted state. | Wrap multi-query deletion and update sequences in `BEGIN; ... COMMIT;` with `ROLLBACK` on catch block. | Partial deletion leaving dangling foreign key references and data inconsistency. | Low; standard database transactional hygiene. |
| **15** | 🟡 Medium | **CORS Configuration** | [server/src/app.js:21](file:///c:/HRMS/server/src/app.js#L21) | Wildcard CORS `app.use(cors({ origin: "*" }))` without origin validation. | In `app.js:21`, `cors({ origin: "*" })` allows any domain to issue cross-origin requests. | In production, whitelist `https://attendance-cgs.vercel.app`, local dev origins, and allow custom company domains stored in `company_domains` table. | Potential CSRF or unauthorized cross-origin integration. | Low; whitelists Vercel production + localhost + custom company domains. |
| **16** | 🟡 Medium | **Error Handling & Leaks** | [server/src/controllers/userController.js:194](file:///c:/HRMS/server/src/controllers/userController.js#L194) | Database error leakage to client: `res.status(500).json({ error: "Failed to delete user: " + err.message })`. | Raw error strings with database table and constraint names sent to client. | Standardize error messages. Log detailed error server-side, return safe generic message to client. | Exposes database schema and internal error messages to attackers. | Zero. Better security hygiene. |
| **17** | 🟡 Medium | **WebSocket Room Auth** | [server/src/index.js:22-24](file:///c:/HRMS/server/src/index.js#L22-L24) | Unauthenticated Socket.io room join: `socket.on("join_company", (companyId) => { socket.join("company_" + companyId); })`. | Any WebSocket connection can join any company's room and receive real-time events. | Validate socket handshake token or verify user belongs to `companyId` before calling `socket.join()`. | Cross-tenant eavesdropping of live chat or attendance punch events via WebSockets. | Low; requires passing auth token in socket handshake. |
| **18** | 🟡 Medium | **Input Validation** | [server/src/controllers/authController.js:228-236](file:///c:/HRMS/server/src/controllers/authController.js#L228-L236), [server/src/controllers/leaveController.js:94-110](file:///c:/HRMS/server/src/controllers/leaveController.js#L94-L110) | Missing date and format validation on write endpoints (e.g. invalid date strings, negative days). | While some string parsing exists, invalid dates or non-numeric days can cause unexpected DB errors. | Add validation helper to verify ISO date strings (`YYYY-MM-DD`) and positive numeric days before querying. | 500 unhandled errors on malformed payloads. | Zero. Improves API stability. |
| **19** | 🟢 Low | **Dead Packages** | [client/package.json:19-20](file:///c:/HRMS/client/package.json#L19-L20) | `firebase` and `firebase-admin` packages in `client/package.json` are largely unused since `firebase.js` was converted to REST API client. | Grep confirmed only `RaiseTicketModal.jsx` connects to external support ticketing project `zuna-landing-page-22564`. Core HRMS has zero Firestore dependency. | Retain support for `RaiseTicketModal.jsx` or tag for subsequent lightweight ticket API migration. | ~500 KB unnecessary bundle size. | Medium if removed hastily; keep `RaiseTicketModal.jsx` intact. |
| **20** | 🟢 Low | **Structured Logging** | [server/src/services/emailService.js](file:///c:/HRMS/server/src/services/emailService.js), [server/src/app.js](file:///c:/HRMS/server/src/app.js) | Server logs use unstructured `console.log` without request IDs, latency tracking, or standardized error format. | Server inspection shows raw `console.log` statements. | Add request logging middleware showing method, path, status, and duration (e.g. `[GET] /api/health 200 - 2ms`). | Slower diagnosis of production incidents. | Zero. |
| **21** | 🟢 Low | **Scripts Cleanup** | [server/scripts/check-attendance-cols.js](file:///c:/HRMS/server/scripts/check-attendance-cols.js) | Temporary scratch script created during audit. | Created in `server/scripts/check-attendance-cols.js`. | Clean up after audit completion. | Minor clutter. | Zero. |

---

## 3. Implementation Plan & Phases

To adhere strictly to the **Code Change Safety Rules** and the **Evidence-Gated Protocol**:
1. **Phase 2 — Security & Data-Safety Fixes:**
   - Fix #1: Correct syntax typo in `client/src/pages/Profile.jsx:9` and verify `vite build` passes.
   - Fix #2: Eliminate unauthenticated `companyId` admin fallback in `server/src/middlewares/auth.js`.
   - Fix #3, #4, #5: Add tenant boundary checks and role-based permissions in `userController.js` (`getUsers`, `updateUser`, `deleteUser`).
   - Fix #6: Add participant check in `chatController.js` (`getDirectMessages`, `getDmThreads`).
   - Fix #7: Fix `total_working_minutes` ➔ `duration_minutes` bug in `regularizationController.js`.
   - Fix #8: Enforce manager/admin approval on `updateLeaveStatus`.
   - Fix #9: Protect `payroll` deletion and modification endpoints with `requireAdmin`.
   - Fix #10: Restrict `updateCompany` to admins of that company.
   - Fix #11: Protect `externalLinks` management routes with `authenticateToken`.
   - Fix #12: Add in-memory rate limiting middleware for auth endpoints.
2. **Phase 3 — Data & Module Integrity:**
   - Fix #14: Wrap multi-step deletions (`deleteUser`, `deleteProject`) in database transactions (`BEGIN / COMMIT / ROLLBACK`).
   - Fix #16: Standardize error handling without database detail leakage.
   - Fix #18: Add input validation for dates and positive numeric days.
3. **Phase 4 — Reliability & Operational Tests:**
   - Run automated verification scripts across Auth, Tenant Isolation, Attendance, Leaves, Payroll, and Chat.
   - Execute full client production build (`vite build`).
   - Verify server syntax and health endpoint.
